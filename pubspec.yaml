name: firebasetest
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  flutter_dotenv: ^5.1.0
  # connecter la base de données Firebase
  # firebase_core: ^3.0.0

  # manipuler les données dans firebase
  # cloud_firestore: ^5.0.0

  http: ^1.2.1
  multi_select_flutter: ^4.1.3
  youtube_player_flutter: ^9.0.1
  flutter_rating_bar: ^4.0.1

  #  créer des objets fictifs qui simulent le comportement d'objets réels
  mockito: ^5.4.4

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

 
  flutter_lints: ^4.0.0


flutter:


  uses-material-design: true



  assets:
    - .env
    - assets/images/


